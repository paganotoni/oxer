package folders

import (
	"errors"
	"oxer/internal/meta"
)

// Move render to be its own package with the helpers
func MoveRender(app meta.Application) error {
	return errors.New("not implemented")
}
